{
  "builds": [
    {
      "src": "entry_points/vercel_handler.py",
      "use": "@vercel/python"
    },
    {
      "src": "index.html",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "entry_points/vercel_handler.py"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ],
  "installCommand": "pip install poetry && poetry export -f requirements.txt --output requirements.txt --without-hashes && pip install -r requirements.txt"
}